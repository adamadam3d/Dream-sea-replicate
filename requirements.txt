# Core Machine Learning
torch
torchvision
torchaudio
transformers
# IMPORTANT: diffusers must match the version used to TRAIN the checkpoints.
# Different releases rename internal UNet submodules, which breaks weight loading
# (load_state_dict in generation_inpainting.py now fails loudly on a mismatch).
# If checkpoint loading errors, change this pin to your training version.
diffusers==0.27.2
accelerate

# Data Processing & Math
numpy
scikit-learn
Pillow
scipy

# Utilities
tqdm
packaging
pathlib

# Real differentiable Gaussian rasterizer for faithful multi-view SDS. This is now
# the DEFAULT SDS renderer in generate_3dgs.py (--rasterizer gsplat). CUDA toolkit
# required. Only needed when running SDS (sds_iterations > 0); the 'scatter'
# fallback (--rasterizer scatter) needs no extra deps. Install with: pip install gsplat
gsplat
