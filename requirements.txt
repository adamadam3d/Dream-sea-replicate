# Core Machine Learning
torch
torchvision
torchaudio
transformers
# IMPORTANT: diffusers must match the version used to TRAIN the checkpoints.
# Different releases rename internal UNet submodules, which breaks weight loading
# (load_state_dict in generation_inpainting.py now fails loudly on a mismatch).
# If checkpoint loading errors, change this pin to your training version.
diffusers==0.27.2
accelerate

# Data Processing & Math
numpy
scikit-learn
Pillow
scipy

# Utilities
tqdm
packaging
pathlib
